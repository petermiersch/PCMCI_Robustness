defaults:
  - _self_
  - io: production # test or production
  # disabling hydra output
  - override hydra/hydra_logging: disabled
  - override hydra/job_logging: disabled

# disabling hydra output
hydra:
  output_subdir: null
  run:
    dir: .

